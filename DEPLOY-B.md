# B 前台 v10：單張合影預覽

解壓縮後把全部檔案及 assets/ 覆蓋到 peysonltd-dot/AIimageliveprintB 根目錄；不要只上傳 ZIP。先更新 B 後端 v10，再更新本前台，最後 Ctrl+Shift+R 清除快取。
固定使用 https://aicamera-backend-b.onrender.com；A 機兩個專案不用更動。

流程：選 IP 圖片 → 拍照 → 生成一張合影 → 預覽確認 → 工作人員下載印製。
- 保留六個 IP 圖片選項及選取外框，不顯示角色卡片文字。
- 只顯示一張合影，不需點選款式；圖片下方沒有「超 Q 互動版」等版本名稱。
- 保留放棄與確認按鈕，避免未確認就送出列印。
- 需要人工檢查時，前台只顯示任務號碼及請工作人員協助的提示，確認按鈕停用。處理完成後同一頁會自動更新，不用再拍照。
- 後台可下載待確認圖、重新處理已保留原圖、補傳圖片。PNG 待確認圖另有「檢查完成，提供賓客預覽」；未去背的 JPG 不可直接放行。
- QR 下載頁只提供已確認且可用的圖檔。

通過本機模擬 API 與 DOM 流程測試不代表已實際付費產圖。部署後請用一筆新任務檢查相機、人物/IP 品質及透明列印檔。詳情見後端 DEPLOY-B.md。
