# B 前台 V11：淺藍海洋介面

本包只更新 B 前台，沿用目前 V10 後端的產圖、去背及單張合影設定，不需要更新 Render，也不需改金鑰。

## 上傳位置
解壓縮後，把內容覆蓋到 peysonltd-dot/AIimageliveprintB 根目錄。請上傳檔案，不要只放 ZIP。
需要變更的檔案為：
- index.html
- download.html
- assets/ocean-theme.css（新增）
- assets/boat.svg（新增）
完整包也包含原有角色圖、清單及未改動的 admin.html，方便整包上傳。
完成後用 Ctrl+Shift+R 或無痕視窗開啟前台。

## 本版外觀
- 淺藍天空、海洋漸層、白色卡片與深藍文字，取代深色霓虹背景。
- 前台及手機下載頁移除底部合作單位 LOGO。
- 拍照畫面移除「黃色耳機夥伴」等角色名稱，只保留拍照說明。
- 等待畫面使用既有方舟與所選 IP 圖片，加入船身上下輕搖、海浪流動及小圓點動畫。這些是前端動畫，不增加 AI 請求或產圖費用。
- 支援減少動態效果的系統偏好；此設定下動畫靜止，流程仍可使用。
- 六個 IP 圖片選項、拍照、單張合影預覽、確認送出及 QR 下載流程沿用 V10。

A 機兩個專案均不需更新。本包沒有後端程式，不會改變生成圖案或輸出檔案。
